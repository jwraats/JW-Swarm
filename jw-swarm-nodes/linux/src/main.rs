//! JW Swarm inference node agent (Linux).
//!
//! Scaffold for Phase 3 (P3). The full agent — outbound WSS+mTLS tunnel,
//! catalog fetch + model download, backend manager, scheduler, and metrics —
//! is implemented here. For now this is a placeholder entry point so the
//! package and CI pipeline build end-to-end.

fn main() {
    println!(
        "jw-swarm-node {} — not yet implemented (see DESIGN.md Phase 3)",
        env!("CARGO_PKG_VERSION")
    );
}
